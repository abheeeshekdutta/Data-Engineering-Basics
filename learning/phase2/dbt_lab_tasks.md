# Phase 2 Lab Tasks (dbt Core)

1. Initialize dbt project and configure profile.
2. Build all `stg_*` models from raw sources.
3. Add tests: unique, not_null, relationships.
4. Build `int_orders_enriched` logic.
5. Build marts (`dim_customers`, `dim_products`, `fct_orders`).
6. Add customer snapshot for SCD behavior.
7. Add exposures for dashboard dependencies.
8. Run `dbt build` and resolve all test failures.
9. Generate docs and review lineage graph.
10. Perform fresh-clone reproducibility run.
